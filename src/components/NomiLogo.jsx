import React from "react";

export default function NomiLogo() {
return (
    <div className="flex items-center gap-2 select-none">
    {/* 4 pequeños iconos decorativos mini */}
    
    {/* Texto de la marca */}
    {/* Logotipo principal unificado */}
    <span className="text-xl font-black text-[#2A3447] tracking-tight flex items-center leading-none">
        N
        <span className="inline-flex items-center justify-center w-5.5 h-5.5 rounded-full bg-[#FCD3D7] text-[#BD6B6B] mx-0.5 text-xs font-black shadow-xs">
        ✓
        </span>
        MI
    </span>
    </div>
);
}